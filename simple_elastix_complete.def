BootStrap: docker
From: chunglabmit/simpleelastix:latest

%help
Base with simpleelastix and env named py38 with goodies

%runscript
    echo "Installing and making simple elastix from local image with conda already in it."

%environment
    CONDA_DIR=/opt/conda
    CONDA_SRC=/opt/conda/etc/profile.d/conda.sh
    export CONDA_DIR CONDA_SRC

%post
    apt-get update -qq
    apt-get install -y -q --no-install-recommends \
	 wget \
         vim \
	 bzip2 \
         unzip python3-opencv ca-certificates
    wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh --ca-directory=/usr/ssl/certs -O ~/miniconda.sh
    bash ~/miniconda.sh -b -p /opt/conda
    echo 'export PATH=/opt/conda/bin:/opt/conda/condabin:$PATH' >> $SINGULARITY_ENVIRONMENT
    . /opt/conda/etc/profile.d/conda.sh
    conda create -n py38 python=3.8 -y
    conda activate py38
    python /build/SimpleITK-build/Wrapping/Python/Packaging/setup.py install
    conda install scipy ipykernel numpy pandas matplotlib \
    	 scikit-learn scikit-image -y
    conda install -c conda-forge jupyterlab -y
    pip install opencv-python-headless
    python -m ipykernel install --name py38_sing --display-name "Python (py38_sing)"
    conda clean --all
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*
    rm ~/miniconda.sh
